<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/BarberiaVikingos/logo2.svg" />
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />

		<title>Barbería Los Vikingos</title>

        <!-- fuente Safira March -->

		<style>
           @font-face{
			font-family: 'Safira March';
			src: url('/BarberiaVikingos/Fonts/Safira March Personal Use Only.ttf');
		   }
		</style>
	</head>

	<body>
		<slot />
	</body>
	
</html>

<!-- Global Styles  -->
 <!-- is:global para que admita los stilos -->
<style is:global>
	
	body{
		position: relative;
	    margin: 0;
		padding: 0;
		background-color: #FF4D00;
		font-family: 'Safira March', serif;
		color: #000000;
		overflow-x: hidden;
    }
	body::before,
	body::after{
		content: '';
		position: absolute;
		bottom: 0;
		width: 150px;
		height: 416px;
		margin-left: 70px;
		margin-right: 70px;
		opacity: 0.5;
		background-image: url('/BarberiaVikingos/img/footer2.png');
		background-repeat: no-repeat;
		background-size: cover;
		z-index: -1;
	}
	body::before {
		left: 0; /* Lado izquierdo */
	}
	body::after {
		right: 0; /* Lado derecho */
	}
	*{
		box-sizing: border-box;
	}
	.container{
        max-width: 1100px; 
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        padding: 10px 20px;
        background-color: #fff;
		padding-bottom: 10px;
		position: relative;
		z-index: 1;
    }
	section {
      background-color: #ffffff;
	  position: relative;
    }
	img {
      max-width: 100%;
      height: auto;
      display: block;
      object-fit: cover;
    }
</style>
